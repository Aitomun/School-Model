package Models;

import Enums.Gender;

public class Principal extends Persons{
    public Principal(int age, String firstName, String lastName, Gender gender) {
        super(age, firstName, lastName, gender);
    }
}
